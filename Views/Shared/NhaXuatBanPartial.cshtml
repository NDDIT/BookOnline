
<div class="list-group">
    <p class="list-group-item active" style="font-weight:bold; text-align:center">SÁCH THEO NHÀ XUẤT BẢN</p>
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SPTheoNhaXuatBan", "BookStore", new { id = item.MaNXB })" class="list-group-item">@item.TenNXB</a>
    }
</div>

